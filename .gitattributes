# Normalize line endings across agent/dev platforms; check out LF everywhere.
* text=auto eol=lf

# Keep generated/lock artifacts from skewing diffs and language stats.
# (uv.lock is intentionally NOT marked generated — its diffs are reviewed.)
*.png binary
*.jpg binary
